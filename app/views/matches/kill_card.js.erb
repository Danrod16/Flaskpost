const activeCard = document.querySelector(".active")
console.log(activeCard);

const goToNextCard = () => {

};

const kill_card = () => {
  activeCard.innerHTML = "";
  goToNextCard();
};

goToNextCard();
