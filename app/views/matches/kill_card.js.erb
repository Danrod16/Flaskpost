let activeCard = document.querySelector(".active")
let nextCard = activeCard.nextSibling.nextSibling

console.log(activeCard);

const goToNextCard = () => {
  nextCard.classList.add("active");
};

const kill_card = () => {
  activeCard.remove();
  goToNextCard();
};

kill_card();
